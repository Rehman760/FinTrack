{
  "name": "fintrack-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "postbuild": "cp -r client/build server/build || xcopy /E /I client\build server\build",
    "start": "node server/index.js"
  }
}